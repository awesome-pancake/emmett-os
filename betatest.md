## Non-Compsci Person
- Successfully used rainbow, cls, echo, help
- Found command arguments for poke and peek to be unclear
    - Suggested including background information about computer memory
    - Alternatively suggested improved documentation with a description of each command and its arguments

## Compsci Person
- Used environment variables in the same way (typed directly into the console without echo command and expected to print)
    - I will have to modify the way environment variables display to be more intuitive for the user
- Discovered a known bug with printing at the end of the console:
    - Typing the help command at the end of the console has the result of causing a triple fault
